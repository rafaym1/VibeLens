{# Rationale generation user prompt (L4) #}
## User Profile

{{ user_profile | tojson(indent=2) }}

## Candidates

For each candidate below, write a personalized rationale explaining why it matches this user's workflow.

{% for candidate in candidates %}
- **{{ candidate.name }}** (id: `{{ candidate.item_id }}`): {{ candidate.description }}
{% endfor %}

Produce the rationale JSON array with item_id, rationale, and confidence for each candidate. Use the exact item_id shown in backticks above.
