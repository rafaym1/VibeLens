## What is a Skill?

A skill is a `SKILL.md` file Claude loads on demand. Its YAML frontmatter has exactly two required fields — `name` and `description` — and the markdown body contains the procedural instructions.
